#[derive(Debug)]
pub enum Token {
    Let,
    Eq,
    Semi,
    Plus,
    Int(i32),
    Lparen,
    Rparen,
    Lbrace,
    Rbrace,
    Fun,
    FatArrow,
    ThinArrow,
    Colon,
    Comma,
    Dot,
    Type,
    String(String),
    Id(String),
    QuoteChar(String),
    QuoteMarker,
    Splice(String),
}
