#[derive(Debug)]
pub enum Token {
    Let,
    Eq,
    Semi,
    Plus,
    Int(i32),
    Lparen,
    Rparen,
    Lbrace,
    Rbrace,
    Fun,
    Arrow,
    String(String),
    Id(String),
    QuoteChar(String),
    QuoteMarker,
    Splice(String),
    TyPrimitive(String),
}
